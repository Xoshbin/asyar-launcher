<script lang="ts">
  import type { ILogService } from 'asyar-sdk';

  interface Props {
    logger: ILogService;
  }
  let { logger }: Props = $props();

  // The extension can pass context via URL params before navigating here
  const params = new URLSearchParams(window.location.search);
  let selectedTitle = $state(params.get('title') ?? 'Detail View');
</script>

<div class="container">
  <h1>{selectedTitle}</h1>
  <p class="mb-6" style="color: var(--text-secondary); font-size: var(--font-size-sm);">{{EXTENSION_NAME}}</p>

  <div class="flex-1">
    <p style="color: var(--text-secondary);">Replace this with your detail content.</p>
  </div>
</div>

<style>
  .container {
    padding: var(--space-6);
    height: 100%;
    display: flex;
    flex-direction: column;
    background: var(--bg-primary);
    color: var(--text-primary);
    font-family: var(--font-ui);
  }
  h1 {
    font-size: var(--font-size-xl);
    font-weight: 600;
    margin-bottom: var(--space-1);
  }
</style>
