{
  "name": "asyar-calculator-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsup index.ts --format iife --global-name ExtensionBundle --no-splitting --clean --external asyar-api"
  },
  "dependencies": {
    "mathjs": "^12.4.3"
  },
  "devDependencies": {
    "asyar-api": "link:../../asyar/asyar-api",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
