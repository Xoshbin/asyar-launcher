{
  "id": "store",
  "name": "store",
  "description": "Browse and install Asyar extensions.",
  "version": "1.0.0",
  "author": "Asyar",
  "icon": "icon:store",
  "type": "extension",
  "searchable": true,
  "permissions": ["notifications:send"],
  "commands": [
    {
      "id": "browse",
      "name": "Browse Extension Store",
      "trigger": "store",
      "description": "Find and install new extensions",
      "icon": "icon:store",
      "mode": "view",
      "component": "DefaultView"
    }
  ],
  "walkthrough": [
    {
      "id": "install-an-extension",
      "title": "Add something Asyar does not do yet",
      "summary": "Install your first extension from the Store",
      "icon": "icon:store",
      "order": 80,
      "body": "Extensions are built on the same API the built-in features use, so anything in the Store behaves like it shipped with the app — same search, same actions, same keyboard rules.\n\n## To complete the task\n\nOpen the **Store**, find something you would actually use, and install it.",
      "completion": {
        "type": "state",
        "probe": "extensions.installedCount",
        "atLeast": 1
      }
    }
  ]
}
